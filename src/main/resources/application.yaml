## jpa ??
spring:
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update # ??? update create(drop??? ?? ??)
    properties:
      hibernate.format_sql: true # sql ??? ??? ??
  datasource:
    url: jdbc:mariadb://localhost:3306/sns
    username: root
    password: 123456
    driver-class-name: org.mariadb.jdbc.Driver




jwt:
    secret-key: fast_campus.sns-application-2002.secret_key
    #30 days
    token.expired-time-ms: 2592000000
